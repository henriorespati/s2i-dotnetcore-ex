spec:
  containers:
  - name: jnlp
    env:
    - name: CONTAINER_ENV_VAR
      value: jnlp
  - name: dotnet
    image: image-registry.openshift-image-registry.svc:5000/openshift/dotnet:6.0
    env:
    - name: CONTAINER_ENV_VAR
      value: dotnet